@{
    ViewData["Title"] = "Home Page";
}
@{
    string user = ViewBag.Usuario;
    string rol = ViewBag.Rol;
}
<div class="text-center">
    <h1 class="display-4">Welcome @if (!string.IsNullOrEmpty(user)) {
    @user
        }
 de rol @if (!string.IsNullOrEmpty(rol)) {
    @rol
        }
 </h1>
</div>